<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" >

    <!-- Login Page-->
    
    <LinearGradientBrush x:Key="LoginPageBackgroundBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#000428" Offset="0.0" />
        <!-- Midnight Blue -->
        <GradientStop Color="#004e92" Offset="0.5" />
        <!-- Deep Sky Blue -->
        <GradientStop Color="#00c6ff" Offset="1.0" />
        <!-- Light Cyan -->
    </LinearGradientBrush>

    <!-- Login Button Style -->
    <Style x:Key="LoginButtonStyle" TargetType="Button">
        <Setter Property="TextColor" Value="#FFFFFF" />
        <Setter Property="BackgroundColor" Value="#000428" />
        <Setter Property="FontFamily" Value="OpenSansRegular" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Padding" Value="14,10" />
        <Setter Property="MinimumHeightRequest" Value="44" />
        <Setter Property="MinimumWidthRequest" Value="44" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor" Value="#6E6E6E" />
                            <Setter Property="BackgroundColor" Value="#404040" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>
 
    
    
<!--home page-->


    <!-- Home Page Background Brush -->
    <!-- Home Page Background Brush -->
    <LinearGradientBrush x:Key="HomePageBackgroundBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#EAF2F8" Offset="0.0" />
        <!-- Soft Light Blue -->
        <GradientStop Color="#D4E4F2" Offset="0.5" />
        <!-- Muted Blue-Gray -->
        <GradientStop Color="#B8D0E8" Offset="1.0" />
        <!-- Subtle Blue -->
    </LinearGradientBrush>

    <!-- Colors -->
    <Color x:Key="PrimaryTextColor">#0056B3</Color>
    <!-- Dark Gray for better readability -->
    <Color x:Key="AccentColor">#FFFFFF</Color>
    <!-- Rich Blue for accents -->

    <!-- Border Style -->
    <Style x:Key="HomePageBorderStyle" TargetType="Border">
        <Setter Property="StrokeShape" Value="RoundRectangle 16" />
        <Setter Property="BackgroundColor" Value="{StaticResource AccentColor}" />
        <!-- Rich Blue -->
        <Setter Property="Padding" Value="15" />
        <Setter Property="Shadow">
            <Setter.Value>
                <Shadow Brush="#888888" Opacity="0.5" Offset="5,5" />
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Shared Label Style -->
    <Style x:Key="BaseLabelStyle" TargetType="Label">
        <Setter Property="HorizontalOptions" Value="Center" />
        <Setter Property="TextColor" Value="{StaticResource PrimaryTextColor}" />
    </Style>

    <!-- Label Style -->
    <Style x:Key="HomePageLabelStyle" TargetType="Label" BasedOn="{StaticResource BaseLabelStyle}">
        <Setter Property="FontSize" Value="18" />
        <Setter Property="FontAttributes" Value="Bold" />
    </Style>

    <!-- Icon Label Style -->
    <Style x:Key="HomePageIconStyle" TargetType="Label" BasedOn="{StaticResource BaseLabelStyle}">
        <Setter Property="FontFamily" Value="FontAwesomeSolid" />
        <Setter Property="FontSize" Value="48" />
        <Setter Property="TextColor" Value="{StaticResource PrimaryTextColor}" />
    </Style>

</ResourceDictionary>